# THIS FILE IS AN EXAMPLE ONLY — never commit the real secret.
# Create out-of-band:
#
#   kubectl create secret generic backend-secret \
#     --namespace taskapp \
#     --from-literal=SECRET_KEY="$(openssl rand -hex 32)"
---
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: taskapp
type: Opaque
stringData:
  SECRET_KEY: REPLACE_ME
