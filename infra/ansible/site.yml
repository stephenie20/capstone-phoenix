---
- name: Harden all nodes
  hosts: k3s_cluster
  become: true
  roles:
    - hardening

- name: Install k3s server on control-plane
  hosts: control_plane
  become: true
  roles:
    - k3s-server

- name: Join workers to the cluster
  hosts: workers
  become: true
  roles:
    - k3s-agent